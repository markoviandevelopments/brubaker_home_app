name: brubaker_homeapp
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_rating_bar: ^4.0.1
  http: ^1.4.0
  flutter_colorpicker: ^1.1.0
  google_fonts: ^6.2.1
  firebase_core: ^4.0.0
  cloud_firestore: ^6.0.0
  firebase_storage: ^13.0.0
  firebase_auth: ^6.0.0
  provider: ^6.1.2
  image_picker: ^1.0.7
  video_player: ^2.9.1
  chewie: ^1.7.5
  uuid: ^4.4.2
  shared_preferences: ^2.3.2
  crypto: ^3.0.5
  animate_do: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1
  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true
  assets:
    - assets/images/restaurants/

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon.png"
```<grok-card data-id="6c9e19" data-type="citation_card"></grok-card><grok-card data-id="45cd22" data-type="citation_card"></grok-card><grok-card data-id="3bf8bf" data-type="citation_card"></grok-card><grok-card data-id="1f3b6f" data-type="citation_card"></grok-card>

Run `flutter clean && flutter pub get`, commit/push, and re-trigger the workflow. Yeehaw from Houston—keepin' it crisp and modern!
